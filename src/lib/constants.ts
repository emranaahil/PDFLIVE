/** Strict 10 MB gate — check File.size BEFORE FileReader / arrayBuffer(). */
export const MAX_FILE_BYTES = 10_485_760;

/** Print check is parked. Set true to restore scan + fix-for-print. */
export const PRINT_CHECK_ENABLED = false;

export const ACCEPTED_PDF = "application/pdf,.pdf";

export const JPEG_COMPRESS_QUALITY = 0.6;

export const PRINT_LUMINANCE_THRESHOLD = 200;
